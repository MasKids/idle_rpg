# 변경 이력

이 프로젝트의 주요 변경사항을 기록합니다.

## [Unreleased]

### 추가
- 플레이어 프로필 상태 — 닉네임(`playerName`), 누적 플레이타임(`totalPlayTime`,
  리버스해도 유지), 현재 회차 플레이타임(`currentRunTime`, 리버스 시 0으로 리셋)을
  게임 상태에 추가했다. 둘 다 타임스탬프 기반으로 계산해서 앱을 껐다 켜도 정확히
  이어지고, 앱이 꺼져 있던 시간(오프라인)은 더해지지 않는다. 세이브에도 포함됨.
  아직 이 값을 보여주는 화면은 없다(상태만 준비).
- 스테이지 최초 클리어 다이아 보상 — 계정 기준으로 각 스테이지를 처음 깰 때만
  다이아를 지급한다(`StageTable.FirstClearDiamond`, 챕터가 오를수록·보스일수록
  증가). 이미 깬 적 있는 스테이지는(리버스로 다시 지나가도) 재지급되지 않는다 —
  기존에 실시간으로 갱신되던 `rebirthMaxStage`(최고 도달 스테이지, 리버스해도
  유지)를 그대로 판정 기준으로 재활용해 별도 상태 없이 구현했다. 최초 클리어
  순간에는 일반 처치 데미지 숫자와 구분되는 금색 다이아 배지가 1.8초간 뜬다.

### 변경
- 밸런싱 테이블 구조 개편 2단계 — 존재력 트리(50노드)·스테이지(200개)·무기(75종)를
  실시간 공식 계산 대신 엑셀에 리터럴 값으로 직접 나열하도록 바꿨다. 플레이 경험
  자체는 그대로지만, 이제 노드·스테이지·무기 하나하나를 엑셀에서 개별로 손볼 수
  있다(예: 특정 무기만 세게, 특정 스테이지만 난이도 조정). 콘텐츠 확장에 대비해
  스테이지는 기존 10챕터가 아니라 20챕터(200개)까지 미리 채워뒀다.
- `WeaponTypeTable`/`MasteryTable` 정리 — 무기 종류별 특성 스탯(검=ATK/창=ASPD/
  활=CRIT)을 `WeaponTable` 각 행으로 옮기고 `WeaponTypeTable`을 없앴다. `MasteryTable`
  3행이 사실상 전부 같은 값이었던 걸 확인하고 삭제, 값은 `CommonTable`과 코드
  상수로 흡수했다. 플레이 경험은 동일하다.
- 1행짜리 테이블 4개(`WeaponUpgradeTable`/`WeaponFusionTable`/`TimeHeistTable`/
  `RebirthTable`) 정리. `TimeHeistTable`은 사용 횟수별 10행으로 펼쳐 비용/쿨타임/
  보상 배율을 개별 조정할 수 있게 됐고, 나머지는 값이 성격에 맞게 `CommonTable`·
  `CurrencyTable`·`RebirthRewardTable`로 흡수됐다(리버스 시 재화 환급 여부가 이제
  실제로 `CurrencyTable`을 참조한다). 플레이 경험은 동일하다.
- StringTable 확장 — 그동안 화면 컴포넌트에 직접 박혀 있던 한글 문구(특히 리버스
  설명 화면)를 전부 StringTable로 옮기고, 무기 75종·존재력 노드 50개·유물 9종의
  이름/설명을 엑셀에서 관리할 수 있게 했다(이름은 아직 자동 생성 placeholder라
  나중에 엑셀에서 실제 이름으로만 교체하면 됨). 플레이 화면에 보이는 텍스트는
  그대로다.
- `WeaponGradeTable`/`GradeTable` 정리 — 무기 종류를 `WeaponTable`에 합쳤던 것과
  같은 방식으로, 등급 이름·색상 정보도 무기/유물 각 행에 직접 두도록 바꿨다(등급별
  기준 배율은 이미 다른 값에 반영돼 있어 정리하며 함께 제거). 무기고·유물·가챠
  화면의 등급 배지 색상과 이름은 이전과 동일하다.
- 화면 비율 고정 — 데스크톱 브라우저 창 크기를 바꿔도 게임 UI가 항상 같은 세로
  프레임 비율(430×900)로 보이게 바꿨다. 창이 짧으면 프레임 전체가 세로에 맞춰
  축소되고(가로만 남아 찌그러지는 문제 해결), 넓으면 화면을 채우도록 확대된다.
  프레임 밖은 은은한 비네트 배경으로 채운다. 실제 모바일 기기(가로 640px 이하)는
  이 제약 없이 화면을 그대로 채운다.
- 전체 진행 속도 하향 — 리버스 해금(15노드)까지 약 3분이던 것을 15~20분대로,
  타임 하이스트 해금(33노드)까지를 40분 전후로 늦췄다. 존재력 트리 해금 비용
  곡선을 더 가파르게 하고(특히 리버스 해금 직전 구간을 집중적으로), 스테이지 적
  HP 증가율을 올렸다 — 다만 초반 스테이지는 원래도 HP가 낮아 첫 5분 체감 속도는
  거의 그대로다. 최초 클리어 다이아 보상이 새 수급원으로 추가된 만큼
  `RebirthRewardTable`의 리버스 지급량은 절반으로 낮춰 다이아 총량이 과해지지
  않게 맞췄다.

### 수정
- 리버스·타임 하이스트 해금 비용이 실제 노드(15번·33번) 비용과 1만큼 어긋나 있던
  자잘한 오차 수정(66→65, 599→598).
- 전투 영역/성장(컨트롤) 영역 비율이 창 크기에 따라 들쭉날쭉하던 문제 수정 — 컨트롤
  영역이 실제 화면 높이의 44%를 차지하도록 돼 있었는데, 화면 비율 고정 작업 이후
  이 기준이 프레임의 실제 크기와 어긋나 창 높이에 따라 전투 영역이 크게 작아지는
  경우가 있었다. 두 영역을 정확히 반반(50:50)으로 맞추고, 몬스터 스프라이트·
  스테이지 배지·체력바도 더 크게 키웠다.

## [v0.1.0] - 2026-09-10

프로토타입 초기 버전. 핵심 3시스템(존재력 트리·리버스·타임 하이스트)과 무기/유물
시스템, 성장 탭, 자동 전투 루프까지 방치형 RPG의 한 사이클을 처음부터 끝까지
플레이할 수 있는 상태.

### 추가

- **존재력 트리** — 50노드 단일 선형 트리. 순서대로 해금하며 스탯 상승/재화 지급
  효과를 얻고, 15노드·33노드 해금 시 트리 옆에 리버스·타임 하이스트 특별 해금이
  등장한다.
- **리버스(REBIRTH)** — 스테이지·스탯·숙련을 초기화하고 무기·유물을 소멸/초기화하는
  대신, 그동안 소비한 재화를 전액 환급받고 도달 스테이지에 비례한 다이아를 새로
  받는다. 존재력 트리는 유지. 리버스 회차가 쌓일수록 환급 배율이 올라간다.
- **타임 하이스트** — 시간에너지를 소비해 미래 스테이지의 보상을 미리 수령. 사용할수록
  비용과 쿨타임이 늘어난다.
- **성장 탭** — 5스탯(공격력/공격속도/치명타확률/치명타피해/존재력획득량) 업그레이드,
  무기 종류별(검/창/활) 숙련. 성장에너지가 들어올 때마다 자동으로 가장 싼 스탯을
  올려주는 자동 업그레이드 토글(시연 편의용, 존재력 트리는 자동화 대상 아님).
- **무기 시스템** — 가챠로 획득(3종류×5등급×5단계, 75종), 레벨업, 돌파, 합성. 보유·
  장착 효과가 분리되어 있고 돌파/합성 모두 최소 1개는 항상 남긴다.
- **유물 시스템** — 뽑기, 슬롯 활성화(존재력 트리 해금 노드 수에 연동), 스탯/골드
  획득량/타임 하이스트 쿨타임 등 다양한 효과.
- **오프라인 보상** — 접속을 끊은 동안 전투가 계속된 것으로 계산해 재접속 시 보상 지급.
- **자동 저장** — 진행 상태를 로컬스토리지에 주기적으로 저장, 새로고침해도 유지.
- **온보딩** — 첫 진입 시 웰컴 안내 3단계, 각 시스템 첫 진입 시 짧은 소개 배너.
- **밸런싱 파이프라인** — 모든 수치를 `balance/balance.xlsx` 엑셀 파일에서 관리하고
  `npm run balance`로 게임 데이터에 반영. 기획자가 코드를 건드리지 않고 엑셀만
  고쳐서 밸런스를 조정할 수 있다.
- **다크 미니멀 디자인 시스템** — 색 토큰, 아이콘, 공용 UI 컴포넌트를 전체 화면에 적용.
- 도감 탭은 "설계 완료 · 프로토타입 미구현" 스텁으로 자리만 확보(의도적 범위 밖).
- Vercel 배포 설정, 프로젝트 소개 README, 아키텍처/무기 시스템/디자인 시스템 문서.

### 변경

- 장비 5부위 강화 시스템(무기/투구/갑옷/장갑/신발)을 폐기하고 무기 가챠·레벨업·
  돌파·합성 구조로 재설계.
- 6스탯 중 실제 전투에 영향을 주지 않던 방어력(DEF)을 제거하고 5스탯 체제로 정리.
- 가챠 탭을 "소환"으로 이름을 바꾸고 무기/유물 하위 탭으로 분리.
- 리버스 회차 보너스를 스탯 직접 증가 방식에서 재화 환급 배율 방식으로 재설계.
- 리버스 시 다이아 지급을 공식 계산에서 도달 스테이지 구간별 고정 지급 테이블로 교체.
- 전체 페이스를 여러 차례 재조정 — 첫 리버스(15노드)까지 5~7분, 타임 하이스트
  해금(33노드)까지 11~15분 선에서 안착.
- 밸런싱 테이블 구조 1단계 개편 — 스탯/숙련 업그레이드 비용 곡선을 공용
  `GrowthCurveTable`로 통합, 재화·등급 속성을 담을 공용 테이블(`CurrencyTable`,
  `GradeTable`) 신설. 밸런스 시딩 스크립트를 1회성으로 격리하고 행 추가 전용 도구를
  마련해, 엑셀에서 직접 수정한 값이 스크립트 재실행으로 사라지지 않게 함.

### 수정

- 전투 루프가 화면(컴포넌트) 언마운트 시 멈추던 문제.
- 리버스해도 최고 도달 스테이지가 실시간으로 갱신되지 않고 다음 리버스 시점에만
  반영되던 문제.
- 플레이어가 강해져 적을 한 방에 처치할 수 있게 되면 체력바가 실제 변화를
  보여주지 않고 멈춘 것처럼 보이던 문제, 그리고 가챠를 연속으로 여러 번 뽑으면
  그동안 전투 체력바 갱신이 밀리던 문제.
- 무기 돌파·합성 시 보유 수량이 0개가 될 때까지 소모되던 문제 — 이제 항상 최소
  1개는 남긴다.
- 리버스 시 성장에너지·골드·숙련의 정수가 초기화되지 않고 기존 보유량에 환급분만
  더해져 리버스할수록 재화가 끝없이 누적되던 문제.
