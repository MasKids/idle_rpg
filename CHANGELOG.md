# 변경 이력

이 프로젝트의 주요 변경사항을 기록합니다.

> ⚠️ **버전이 올라갈 때마다 세이브가 초기화됩니다.** 프로토타입 단계라 구조
> 변경 여부와 무관하게 릴리스마다(v0.X.0/v0.X.Y 모두) 세이브 버전을 함께 올리고
> 마이그레이션은 만들지 않는다(docs/RELEASE.md 3절). 업데이트 후 처음 접속하면
> 진행 상황이 초기화되고, 안내 화면이 한 번 뜬 뒤 이름 입력으로 이어진다 — 이
> 문서의 각 버전 항목에는 더 이상 개별로 표시하지 않는다.

## [Unreleased]

### 변경
- **활/도끼 특화 스탯 전용 밸런스 조정** — 활(치명타 확률 특화)·도끼(치명타피해
  특화)의 `WeaponTable` 보유·장착 효과값을 각각 아래 "치명타 확률/피해 획득량
  하향"과 같은 배율로 낮췄다(검/창/지팡이는 대상이 아님 — PrimaryStat이 달라
  스키마상 자동으로 빠진다).
- **치명타 확률(CRIT) 획득량 절반, 치명타피해(CRIT_DMG) 획득량 10분의 1로
  하향** — StatTable(레벨당 성장치)·ExistTreeTable(트리 노드 수치)·WeaponTable
  (활/도끼 보유·장착 효과)·RelicTable(치명타 계열 유물) 전반에 적용. 다만
  단순히 원천값에 배율을 곱하지 않았다 — 최종 스탯이 (깡스탯 합계) × (1 +
  퍼센트 합계)로 계산되는 구조라, 깡스탯·퍼센트 두 원천에 똑같이 배율을
  곱하면 결과가 제곱으로 더 깎인다(직접 계산해 확인 — 예: 둘 다 ×0.5면
  최종은 ×0.25). 그래서 목표 배율(치확 0.5배/치피 0.1배)의 제곱근을 각
  원천에 곱해, 곱셈 결과가 정확히 목표 배율이 되도록 보정했다. CRIT_DMG의
  StatTable 기본값(150 — 치확 0에서도 크리티컬이 뜨면 1.5배라는 시작값)은
  건드리지 않았다 — 이 값까지 깎으면 치확이 낮은 구간에서 크리티컬이
  논크리티컬보다 약해지는 모순이 생긴다.
  - **검증**: 무기 종류별로 똑같이 투자(같은 등급·단계·레벨·보유 개수)했을
    때의 실질 전투력을 비교한 결과, 중반(Epic3)·후반 초입(Legendary5)
    구간에서는 활/도끼가 검보다 오히려 낮은 DPS를 보였고, 활이 검을 앞서는
    시점은 신화(Mythic) 등급 5단계를 10개씩 보유하는 극단적 엔드게임
    투자뿐이었다(치확이 100%를 여러 번 넘어 다중 치명타가 여러 번 겹치는
    구간). 존재력 트리만으로(무기 없이) 치확 100%/200%에 도달하는 트리
    진행률도 각각 약 34.6%→57.7%, 60.3%→96.2%로 늦춰져, 다중 치명타가
    더는 중반부터 흔하게 터지지 않고 후반부 보상으로 자리 잡았다.
  - 검증 과정에서 창(Spear)의 공격속도(ASPD) 특화 수치가 신화 등급
    다수 보유 시 다른 종류보다 훨씬 크게 튀는 기존(이번 작업 이전부터
    있던) 불균형을 발견했다 — 이번 요청 범위(치명타/활/도끼) 밖이라
    손대지 않았지만 별도로 검토가 필요해 보인다.

## [v0.5.0] - 2026-09-13

v0.4.0에서 크게 상향했던 다이아·존재력 수급을 다시 손보고, 후반부 몬스터 체력이
가파르게 오르도록 조정하고, 막 추가한 다중 치명타의 위력을 낮춘 밸런스 교정
릴리스.

### 변경
- **스테이지 다이아 보상 재조정** — 여러 차례 손봤던 `StageTable.
  FirstClearDiamond`(스테이지 최초 클리어 다이아)를 "스테이지 클리어당 최대
  500을 넘지 않게" 상한을 맞췄다. 당시 최고값(스테이지 300 기준 8,748)에
  균일 배율(500/8748)을 곱해 최댓값이 정확히 500이 되도록 낮췄다(상대 곡선
  모양은 유지). `RebirthRewardTable`(리버스 시 일괄 지급)은 "스테이지
  클리어당" 보상이 아니라는 이유로 범위 밖에 뒀다.
- **스테이지 다이아 최소 지급량 상향** — 위 500 상한 조정 과정에서 초반
  보상(1~9스테이지)이 1개까지 짓눌려 있던 걸 발견해, 최소 50개 ~ 최대 500개
  범위로 다시 맞췄다. 최솟값·최댓값을 각각 목표값에 맞추는 선형 변환이라
  챕터/보스 배율 등 상대적 곡선 모양은 그대로 유지된다.
- **존재력 획득량 30%로 하향** — `StageTable.RewardExist`(처치당 기본
  존재력 보상, 존재력 획득량 스탯이 곱해지기 전 원본값) 전체에 ×0.3 적용.
  존재력 트리 해금·특별 해금 비용 등 소비처는 손대지 않았다 — 참고로 초반
  몇 스테이지(1~8번)는 원래 값이 워낙 작아(1) ×0.3 반올림 결과가 0이
  되는데, 다음 스테이지들이 금방 보상을 채워주고 지나가는 데 몇 초 안
  걸리는 구간이라 별도 최솟값 보정은 넣지 않았다.
- **다중 치명타 위력 하향** — 실제로 체감해보니 다중 치명타(아래 v0.4.0
  참고)가 예상보다 훨씬 강력해서 두 축을 낮췄다. (1) 치명타 변환 20% —
  치확 100% 초과분이 "그대로" 추가 치명타 확률이 되던 걸 20%만 반영하도록
  낮췄다(예: 치확 250% → 예전엔 확정 2회+50%확률로 3회째, 지금은 확정
  1회(100%까지)+초과 150%p 중 20%인 30%p만 반영돼 30% 확률로 2회째).
  (2) 치명타 데미지 보너스 50% — 치명타 배율에서 "100% 기준 보너스분"
  (치명타피해-100)만 절반으로 깎는다(치명타피해 150%면 실제 계산엔 125%로
  반영, 100% 아래로는 안 내려감). 둘 다 계산식 안의 상수
  (`CRIT_CONVERSION_RATE`/`CRIT_DMG_BONUS_SCALE`)만 조정한 것이라
  StatTable 등 밸런스 데이터는 그대로다.
- **후반부 몬스터 체력 지수적으로 상승** — 스테이지 201~300(21~30챕터)의
  `StageTable.EnemyHp`가 챕터를 거듭할수록 오히려 완만해지던 걸(직전
  5챕터 구간의 배율이 37.2배 → 27.1배 → 11.2배로 감쇠) 되돌렸다. 스테이지
  201부터 `growth^((stage-200)/10)`(growth=1.5, 10스테이지마다 ×1.5)
  추가 배율을 곱해 뒤로 갈수록 점점 가팔라지게 만들었다 — 스테이지 300
  기준 최종 배율 약 ×57.7(2.16조 HP)까지. 1~20챕터는 전혀 건드리지
  않았고, 적 공격력·보상 등 다른 칼럼도 그대로 뒀다(요청이 "몬스터 체력"
  으로 한정됨). 너무 급격하다 싶으면 growth 상수 하나만 조정하면 된다.

## [v0.4.0] - 2026-09-13

존재력 트리 250노드·스테이지 300개·무기 150종으로 콘텐츠를 크게 확장하고,
유물·사운드·다중 치명타·일괄 처리(돌파/합성/존재력 해금) 기능을 새로 추가했다.

### 추가
- **다중 치명타** — 치명타 확률이 100%를 넘으면 초과분이 추가 치명타
  판정이 된다. 100%마다 확정 치명타 1회, 남은 소수 부분이 그다음 1회의
  확률, 치명타 배율은 발동 횟수만큼 거듭제곱으로 곱한다(`atk ×
  (치명타피해/100)^발동횟수`). 치확 100% 미만에서는 기존과 완전히 동일하게
  동작(발동 0회 또는 1회뿐). 발동 횟수 상한은 검토했지만 일단 두지 않기로
  했다(아래 "검토" 참고). 스탯 화면에 치확이 100%를 넘으면 "100% 초과분은
  추가 치명타로 적용됩니다" 안내가 뜨고, 전투 데미지 숫자도 2회 이상
  발동하면 글자를 한 단계 더 키우고 "×N"을 붙여 구분한다(색은 그대로 —
  과하지 않게). 출시 직후 위력이 예상보다 강해 너프했다 — v0.5.0 참고.
  - **검토(상한을 두지 않은 이유와 위험)**: 데미지가 치명타피해^발동횟수로
    커지고 발동횟수 자체는 치확에 선형 비례해 늘어나므로, 치확 스탯 하나에
    투자가 몰리면 다른 성장 축(공격력/공격속도 등)보다 훨씬 빠르게
    전투력이 뻥튀기될 수 있다. 지금 당장 상한이나 회차별 배율 체감을 넣진
    않았지만, 나중에 문제가 되면 "발동 횟수 상한"보다는 "2회차부터 배율
    체감"(예: 첫 발동은 그대로, 이후 발동마다 배율 자체를 조금씩 낮춤) 쪽을
    제안한다 — 상한은 그 이상 투자한 치확이 완전히 버려지는 느낌을 주지만,
    체감은 그래도 계속 오르기는 하는 느낌을 준다.
- **일괄 처리 3종** — 무기 상세 모달에 일괄 돌파·일괄 합성, 존재력
  트리에 일괄 해금을 추가했다. 셋 다 같은 패턴: 실행 전 "몇 단계/노드까지
  가는지, 무엇이 얼마나 소모되는지" 미리보기 → 확인 → 실행 후 같은 화면에서
  결과 요약. 재료·재화가 부족하면 실행 버튼이 비활성화되고 이유(지금 처리할
  수 있는 게 없음)가 뜬다.
  - **일괄 돌파(무기 하나)**: 무기 상세 모달에서, 그 무기 하나의 보유
    개수-1(최소 1개 보존)을 재료로 5단계까지 가능한 만큼 반복.
  - **일괄 합성(무기 하나)**: 무기 상세 모달에서, 그 무기부터 5개 단위로
    가능한 만큼 반복. 결과물로 또 합성 가능하면 계속 이어갈지("연쇄 합성")를
    화면에서 토글로 직접 선택하게 했다 — 기본은 꺼짐(한 등급/단계 경계만
    처리). 켜면 각 단계별 소모/획득을 전부 미리 보여준 뒤 실행한다.
  - **무기군 일괄 돌파·합성(무기고 화면)**: 지금 보고 있는 무기 종류(예: 검
    30칸) 전체를 대상으로 돌파·합성 가능한 걸 한 번에 처리한다 — 위 두
    기능이 "무기 하나를 최대한 밀어붙이기"라면, 이건 "이 종류 전체를
    정리하기". 합성은 낮은 등급/단계부터 순서대로 한 칸씩 처리해서(사다리를
    따라 올라가는 순서 자체가 전체를 훑는 효과), 별도 연쇄 토글 없이 한
    번에 사다리 전체가 정리된다. 돌파는 영향받는 무기별로 "N→M단계(소모
    개수)"를 전부 나열해서 보여준다.
  - **일괄 해금(존재력 트리)**: 보유 존재력으로 가능한 만큼 다음 노드부터
    연속 해금. 단, 아직 해금하지 않은 특별 해금(리버스·타임 하이스트) 조건
    노드에 도달하면 그 노드까지만 해금하고 멈춘다 — 특별 해금 자체는 일괄
    대상에서 빼고 항상 개별 클릭으로 하게 했다(놓치면 안 되는 중요한
    순간이라 일괄 처리에 묻히지 않게).
  - 무기 쪽은 `weapon.ts`의 순수 시뮬레이션 함수(`simulateBulkBreakthrough`/
    `simulateBulkFusion`과, 무기군 전체용 `simulateTypeBulkBreakthrough`/
    `simulateTypeBulkFusion`)를 미리보기와 실제 적용(스토어 액션)이 그대로
    공유해서, 미리 보여준 결과와 실제로 벌어지는 일이 어긋날 수 없는 구조로
    만들었다. 존재력 쪽도 `existTree.ts`의 `simulateBulkExistUnlock`이 같은
    역할.
- **몬스터 스프라이트 6종, 로고·로딩 화면 리디자인** — 일반 5종(글리치
  위습·서킷 크롤러·데이터 레이스·프랙처 코어·오실레이터)과 보스 전용 1종
  (널 센티널)을 SVG 컴포넌트로 제작해, 전투 화면의 단색 사각형 적 표시를
  교체했다. 위아래로 은은하게 부유하는 기본 모션에 더해 널 센티널은 바깥
  궤도 링이 천천히 회전하고 오실레이터는 내부 파형이 좌우로 흐른다. 피격
  시 흔들림+플래시, 처치 시 이전 개체가 짧게 페이드아웃하는 연출도 그대로
  연결했다. 보스는 일반 개체의 약 1.75배 크기. 정식 스테이지별 배정
  테이블이 나오기 전까지는 스테이지 번호로 일반 5종을 순환 배정한다(임시).
  모든 색은 하드코딩 없이 `currentColor`/`--color-monster-*` 토큰만 쓰고,
  배경색은 스프라이트에 넣지 않는다(프랙처 코어의 균열은 배경색을 흉내
  내는 대신 mask로 윤곽선을 실제로 뚫어서 표현). `public/sprites/monsters/`
  에 고정 색상 원본 SVG도 참고·보관용으로 함께 저장해뒀다(게임은 이 파일을
  쓰지 않음). 브랜드 로고·로딩 화면도 같은 작업에서 함께 리디자인했다.
- **무기 150종으로 확장** — 75종(검/창/활 × 5등급 × 5단계) → 150종
  (검/창/활/**도끼**/**지팡이** × 6등급 × 5단계). 새 종류 둘은 아직 무기가
  맡지 않았던 나머지 전투 스탯을 채웠다 — 도끼는 치명타피해, 지팡이는
  존재력 획득량이 주 스탯이라 이제 5스탯 전부가 무기 특성으로 대표된다.
  새 등급 **Mythic**(신화)은 Legendary 위, 기존 5색과 겹치지 않는 로즈-레드로.
  기존 75행의 실측 공식(등급마다 ×2, 단계마다 BaseAtk/장착효과는 산술
  +10%·보유효과는 복리 ×1.1)을 그대로 연장해 새 75행을 채웠고, 새 종류의
  장착효과 계수도 기존 3종과 같은 비율(해당 스탯 레벨당 증가량의 약 5배)로
  잡았다. 가챠 확률(6등급 가중치 재배분, Mythic은 Legendary보다 훨씬 희귀하게),
  합성 사다리(종류별 25→30단계), 무기 숙련(3종→5종) 모두 함께 반영. 도끼/지팡이
  전용 라인 아이콘도 새로 그렸다. 1회성 시딩 스크립트:
  `scripts/seed/seed-weapon-expansion.mjs`.
- **스테이지 300개로 확장** — 200개(20챕터) → 300개(30챕터). 존재력
  트리가 250노드로 늘어나며 확인해보니, 200을 넘으면 마지막 행(200번, 보스)으로
  클램프되던 기존 로직 때문에 챕터 표시는 21·22...로 계속 올라가도 실제로는
  같은 보스를 무한 반복하고 있었다(설계 당시부터 "언젠가 채워 넣으면 그만"이라고
  남겨뒀던 자리). 20챕터(191~200번)를 실측해보니 챕터 하나가 다음 챕터로 넘어갈
  때 정확히 같은 배율로 스케일되는 깨끗한 구조라, 21챕터는 그 배율 그대로
  시작해서 30챕터에 가까워질수록 점점 완만하게 낮췄다(HP ×2.06→×1.5, 골드
  ×2.59→×1.7). 존재력 보상만은 예외 — 원래도 스테이지 깊이와 거의 무관하게
  움직이던 값이라(200 스테이지 안에서도 오르내림이 153번, 최대치도 90 근처에서
  안 벗어남) 새로 키우지 않고 기존 패턴을 그대로 이었다. RebirthRewardTable의
  마지막 구간(200~무한)도 200~299 / 300~무한(보상 ×1.8)으로 나눴다. 1회성 시딩
  스크립트: `scripts/seed/seed-stage-table-300.mjs`.
- **존재력 트리 250노드로 확장** — 50노드 → 250노드(5티어×10 →
  25티어×10). 기존 1~50번 노드는 값을 전혀 건드리지 않았고, 51~250번은
  1~50번이 실제로 쓰는 밴드 형태(스탯:재화 = 78:22)를 4번 더 반복하되 5스탯이
  사이클마다 돌아가며 "대표 스탯"을 맡도록 설계해 25티어 전체에 고르게
  분포시켰다. 노드 값은 사이클별 목표 구간 안에서만 완만히 늘어나(가속 없는
  등차수열 형태) 250번째도 급격하게 비싸지지 않는다. 여기 맞춰 리버스 해금
  조건(15→20노드), 타임 하이스트 해금 조건(33→45노드), 유물 슬롯 수(5→8개,
  해금 간격 10→30노드)도 함께 재조정했다. 1회성 시딩 스크립트와 설계 근거는
  `scripts/seed/seed-exist-tree-250.mjs`에 남겨뒀다. 존재력 트리 0~250 구간에서
  "아무것도 새로 열리지 않는" 최대 간격이 27노드(90→117번)였던 걸, 유물 슬롯
  간격을 30노드→25노드로 좁혀 최대 17노드로 줄였다(아래 "유물 15종으로 확장"
  참고).
- **유물 15종으로 확장, 새 등급 "레전드리"** — 9종(Normal/Rare/Epic) →
  15종(Normal/Rare/Epic/Legendary). 새 효과 3종: 성장에너지 획득량 증가,
  시간에너지 획득량 증가, 무기 가챠 신화(Mythic) 등급 확률 상승(가챠 가중치에
  직접 가산). 레전드리 등급은 무기 등급 색상 토큰을 그대로 재사용해(이미
  `Legendary`가 무기 쪽에 있어 새 색상 작업 없이 바로 연결됨) 금색으로
  표시된다. 유물 슬롯도 8개(30노드 간격) → 10개(25노드 간격, 25/50/.../250 —
  존재력 트리 250을 정확히 10등분)로 늘렸다.
- **가챠 레벨 5단계 → 8단계로 확장** — 누적 뽑기 요구치를
  0/50/150/350/700/1300/2300/4000으로 늘려 장기 목표로 삼을 수 있게 했다.
  레벨 0~4의 노말/레어/에픽/유니크 확률은 무기 150종 확장 당시 값 그대로
  유지(아래 "가챠 확률 복원" 참고).
- **가챠 확률 복원 + 신화 확률 재조정** — 신화 확률을 한 차례
  상향했다가 요청에 따라 무기 150종 확장 당시(커밋 5433c49) 분포로
  복원했는데, 그 원본 추세를 그대로 레벨 5~7까지 연장하면 최대 레벨에서
  신화가 4.7%까지 치솟아 버렸다. 신화보다 위 등급이 아직 없는 상태라 "위
  등급이 생기기 전까지는 계속 희귀해야 한다"는 판단으로, 레벨0 0.03% →
  레벨7(최대) 0.100%까지만 완만히 오르도록 다시 설계했다(줄어든 만큼은
  레전드리로 이관 — 신화 바로 아래 등급이 사실상의 "최고 등급" 체감을
  대신 맡는다). 노말/레어/에픽/유니크는 그대로 두었다. 뽑기 비용(50)은
  이 조정과 무관하게 그대로 유지.
- **가챠 확률 표시 소수점 확대** — 등급 확률 표시를 소수점 첫째
  자리 → 셋째 자리로. 신화처럼 낮은 확률이 "0.0%"로 뭉개져 보이던 문제를
  없앴다(예: "신화 0.030%").
- **사운드** — 배경음악 1곡(루프) + 효과음 3종(버튼 클릭/몬스터 피격/
  몬스터 처치), 전부 CC0 무료 음원(Kenney.nl, OpenGameArt.org — 라이선스는
  README.md 참고). `src/lib/audio.ts`에서 Web Audio API로 관리한다. 방치형
  특성상 피격/처치음이 초당 여러 번 겹칠 수 있어 종류별 동시 재생 수 제한
  (클릭 4/피격·처치 3), 최소 재생 간격(20~50ms), 피격·처치음 피치 ±10%
  랜덤화로 반복 재생이 거슬리지 않게 했다. 브라우저 자동재생 정책 때문에
  BGM은 첫 사용자 상호작용(클릭/터치/키 입력) 시점에 시작되고, 탭이
  백그라운드로 가면 자동으로 멈췄다가 돌아오면 다시 재생된다. 프로필 패널에
  설정 섹션(배경음/효과음 볼륨 슬라이더, 전체 음소거 토글)을 추가했고, 값은
  localStorage에 저장돼 재접속해도 유지된다(기본값: 배경음 15%, 효과음 50%,
  음소거 해제).

### 변경
- **세이브 초기화 정책** — 릴리스마다(v0.X.0/v0.X.Y 무관, 저장 구조가 실제로
  바뀌었는지와 무관하게) 세이브 버전을 함께 올려 항상 깨끗한 상태에서
  시작하도록 정했다(docs/RELEASE.md 3절). 손으로 상수를 고치다 빠뜨리는
  사고를 막기 위해 `scripts/bump-save-version.mjs`를 새로 만들어 릴리스
  절차에 넣었다. 안내 문구도 버전마다 따로 적지 않고 CHANGELOG.md 최상단과
  인게임 패치노트 화면 고정 배너로 한 번만 표시한다.
- **다이아 수급량 대폭 상향** — 최초 클리어 다이아 재지급 버그(아래 "수정"
  참고)를 고치는 것만으로도 회차당 수급량이 크게 회복됐지만, 가챠를 더
  여유 있게 돌릴 수 있도록 `StageTable.FirstClearDiamond`/
  `RebirthRewardTable.DiamondReward`를 한 차례 원래 값의 약 9.1배까지
  올렸다가, "너무 퍼준다, 증가폭을 줄일 필요가 있다"는 피드백을 받아 원래
  값(무기 150종 확장 당시, 커밋 5433c49)의 ×4로 낮췄다 — 중간 배율을 거듭
  곱하지 않고 매번 그 원본 커밋 값에서 새로 계산해 반올림 오차가 누적되지
  않게 했다. 가챠 비용은 뽑기당 다이아 40 → 50으로 동결(레벨 무관 고정).
  이후 "스테이지 클리어당 최대 500을 넘지 않게"라는 상한 요청이 한 번 더
  들어와 다시 낮췄는데, 그 조정은 v0.5.0 참고.
- **신화(Mythic) 무기 합성 확인** — 신화1→2→3→4→5 합성이 이미
  가능했음을 확인(등급 배열의 "마지막 등급의 마지막 단계"만 막는 기존 구조라
  Mythic도 자동으로 같은 규칙을 탄다). 코드 변경은 없었고 콘솔로 직접
  재현/검증만 했다.

### 수정
- **리버스 후 최초 클리어 다이아가 다시 지급되지 않던 문제** — "역대
  최고 도달 스테이지"(`rebirthMaxStage`, 리버스해도 초기화 안 됨 — 랭킹·리버스
  보상 구간 판정용)를 "최초 클리어 다이아를 이미 받은 스테이지" 판정에도 그대로
  같이 쓰고 있었다. 그래서 리버스로 스테이지가 1로 돌아가도 이 값은 예전
  회차의 최고 기록에 그대로 남아 있어, 새 회차가 그 기록을 다시 넘어서기
  전까지는 이미 다 지나온 스테이지라도 최초 클리어 다이아가 전혀 나오지
  않았다(v0.2.0에서 "리버스로 다시 지나가도 재지급되지 않는다"고 안내했던 게
  바로 이 동작인데, 의도와 달리 새 회차의 초반 진행 전체가 이 취급을 받고
  있었다). 두 기록을 완전히 분리해(`firstClearMaxStage` 필드 신설, 리버스
  시에만 초기화) 이제 회차마다 처음부터 다시 최초 클리어 다이아를 받는다.
  역대 최고 스테이지 기록 자체는 그대로 유지된다.
- 성장 탭 스탯 카드에 "1,250 = (100 + 400) × 2.5" 같은 내부 계산식이 그대로
  노출되던 문제 — 최종 수치만 보이도록 걷어냈다.
- 로고("Re: N·O·V·A")의 N.O.V.A 글자 간격이 실제 글리프 폭을 반영하지 않고
  좌표를 눈대중으로 잡아 N-O 사이만 다른 글자 사이보다 두 배 가까이 벌어져
  있던 문제. 글자 사이 점(마침표) 위치도 함께 어긋나 있었다 — 실측(getBBox)
  기준으로 세 간격을 균등하게, 점은 각 간격의 정중앙으로 재배치했다.
- 스탯 계산 결과가 밸런스 데이터 이상 등으로 NaN/Infinity가 되면 전투가
  조용히 멈출 수 있던 잠재 위험 — 그런 경우 콘솔에 오류를 남기고 깡스탯만
  적용해 전투가 계속 진행되도록 방어 로직을 추가했다(실제 재현은 못 했다 —
  자세한 내용은 대화 참고).

## [v0.3.0] - 2026-09-11

무기 보유/장착 효과와 스탯 계산을 깡스탯/퍼센트 분리 구조로 전면 개편하고,
게임 제목("Re: NOVA")을 확정했다. 랭킹·이름 입력·프로필 패널·인게임 패치노트
등 플레이 편의 기능도 이 시기에 함께 들어갔다.

### 추가
- **랭킹** — Supabase 연동. 전투 화면 좌측 하단 버튼으로 열람. 리버스를 실행할
  때마다(그 회차 도달 스테이지가 기존 최고 기록보다 높을 때만) 자동으로 기록을
  등록하고(같은 플레이어라도 기존 기록을 덮어쓰지 않고 새 행을 추가), 조회 시
  이름별 최고 기록만 최고 스테이지 내림차순 상위 50명을 보여준다. 리버스를 아직
  해금하지 못한 동안에는 리버스라는 등록 계기가 없어 랭킹에 영영 못 오르는 문제가
  있어, 그 구간에서만 최고 스테이지가 20 단위로 갱신될 때 대신 등록한다(리버스를
  해금하면 이 경로는 멈춘다). 본인 기록은 강조 표시되고, 50위 밖이면 하단에 별도로
  보여준다. `.env`에 Supabase 접속 정보가 없거나 이름이 없으면 각각 랭킹 버튼과
  등록 자체가 조용히 비활성화될 뿐 게임의 다른 부분은 그대로 동작한다(등록 실패도
  조용히 무시되어 전투 진행에 영향 없음).
- **이름 입력 강제** — 새 게임 시작 시(또는 이번 세이브 초기화로 이름이 없어진
  기존 유저) 온보딩 안내보다 먼저 이름 입력 화면이 뜨고, 건너뛰기 없이 2~12자
  (공백만 입력 불가, 앞뒤 공백 제거)를 입력해야 게임에 진입한다. 세이브가 이번에
  초기화된 경우에 한해 그 화면 위에 "저장 데이터가 초기화되었습니다" 안내가 한 번
  먼저 뜬다.
- **프로필 패널** — 전투 화면 좌상단 프로필 칩을 누르면 열린다. 이름 옆 연필
  아이콘으로 언제든 이름을 바꿀 수 있고(같은 2~12자 규칙), 총 플레이 시간·리버스
  횟수도 함께 보여준다. 이름을 바꾸면 이후 랭킹 등록에도 새 이름이 바로 반영된다.
- **랭킹 첫 진입 안내** — 랭킹 버튼을 처음 누르면 등록 조건(리버스 시 기록 갱신될
  때 자동 등록, 리버스 미해금 구간은 20 스테이지 단위 대신 등록)과 등재 조건
  (이름별 최고 기록만 상위 50명)을 설명하는 배너가 한 번 뜬다.
- **패치노트** — 프로필 패널 → 패치노트에서 열람. 버전별로 묶여 최신 버전이
  위에 오고, 항목마다 추가/변경/수정 태그가 붙는다. 못 본 새 버전이 있으면
  패치노트 버튼에 점이 뜨고, 한 번 열어보면 사라진다.
- **게임 제목 "Re: NOVA"(부제 "Nonlinear Oscillation Void Apparatus")** —
  index.html 타이틀/메타 태그, README에 반영. 로고 디자인 시안을 받아
  `BrandLogo` 컴포넌트로 재현했다("Re:" 모노스페이스 박스 + 깜빡이는 커서
  블록, "N·O·V·A" — 마침표는 청록 원, 부제는 자간 넓은 모노스페이스). 색은
  전부 토큰(`--color-brand-teal`/`--color-brand-dim`, 시안 픽셀을 실측해
  값을 맞췄다)이라 다른 곳에서도 재사용 가능. 파비콘도 이 로고 톤에 맞춰
  "커서 블록 2개(채워진 것 + 빈 것)"로 교체했다 — 가로로 긴 로고를 그대로
  쓸 수 없어 만든 정사각 축약형으로, Void(비어있음)와 커서(입력 대기)를
  동시에 표현하며 16×16에서도 뭉개지지 않는다.
- **로딩 화면** — 게임 진입 시 뜨는 조용한 화면. 폰트 로딩이 더 빨리 끝나도
  항상 3초는 강제로 보여준다(최대 6초 안전장치). 중앙에 `BrandLogo`(부제
  포함), 그 아래 얇은 청록 로딩바(좌우 여백 넉넉하게), 그 아래 모노스페이스
  상태 텍스트가 INITIALIZING → LOADING ARCHIVE → READY 순서로 바뀐다.
  배경에는 전투 화면과 같은 동심원 모티프를 아주 흐리게(불투명도 5%) 깔았다.
  스토리 연출이나 타이핑 효과 없이, 로딩이 끝나면 짧게 페이드아웃하고
  게임으로 들어간다. 하단 제작자 표기는 아직 자리표시자.

### 변경
- **리버스 보상 구조 개편** — "누적 소비량 × 환급 배율"(성장에너지/골드/숙련의
  정수를 그동안 얼마나 썼는지 추적해 돌려주는 방식)을 "도달 스테이지 구간별
  고정 지급량 × 리버스 횟수 배율"로 전면 교체했다. 그동안 얼마를 썼는지와 무관하게
  도달 스테이지만으로 지급량이 정해지고, 리버스를 반복할수록(회차당 +5%p, 최대
  ×3.0) 그 지급량이 커진다. 다이아는 예전처럼 배율 없이 구간 고정값만 받는다
  (가챠 재화라 인플레이션 우려로 의도적으로 제외 — 검토 후 결정). 리버스 모달
  표시도 "환급"에서 "리버스 보상"으로 바뀌어 현재 구간·구간 기본 지급량·회차
  배율·최종 지급량·다음 구간과 다음 회차 배율까지 한눈에 보여준다.
- **무기 보유/장착 효과 구조 개편** — 보유 효과가 레벨에
  비례해 커지던 방식을 "레벨·돌파와 무관한 고정값(보유 개수에만 비례)"으로
  바꾸고, 장착 효과는 레벨이 오를수록 커지는 성장 배율(`GrowthCurveTable`의
  등급별 곡선)을 적용해 두 효과의 역할을 명확히 분리했다. 이전에는 보유·장착
  양쪽에 같은 배율(레벨)이 곱해져 상쇄되는 바람에, 창(Spear)·활(Bow)처럼
  기본 공격력 비중이 큰 무기는 레벨을 아무리 올려도 장착이 보유 총합을 못
  따라잡는 문제가 있었다 — 이제 장착만 레벨로 커지므로 레벨업할수록 장착이
  보유보다 더 확실히 앞선다. 무기 상세 화면도 보유 효과는 고정값임을 표시하고,
  장착 효과는 현재 레벨과 다음 레벨 값을 함께 보여주도록 바뀌었다.
- **스탯 계산 구조 개편 — 깡스탯/퍼센트 분리** — 지금까지는
  모든 성장 콘텐츠(6스탯 업그레이드, 존재력 트리, 무기 보유/장착, 무기 숙련,
  유물)가 전부 같은 방식(가산 또는 서로 다른 곱연산)으로 스탯을 올려 콘텐츠
  간 시너지가 없었다. 최종 스탯 = (기본값 + 깡스탯 합계) × (1 + 퍼센트 합계)
  구조로 통일하고, 콘텐츠별 역할을 다시 나눴다: 6스탯 업그레이드·무기 장착
  효과는 깡스탯, 존재력 트리·무기 보유 효과·무기 숙련은 퍼센트, 유물은
  유물마다 다름(혼합). 퍼센트로 바뀐 항목은 전부 수치를 새로 설계했다(기존
  깡수치를 그대로 재사용하지 않음) — 특히 무기 보유 효과는 퍼센트 전환으로
  검/창/활 종류별로 다르게 잡을 필요가 없어져, 이제 등급·단계만으로 정해지고
  세 종류가 동일한 값을 쓴다.
- **재화 획득량 하향** — 퍼센트 곱연산 도입으로 전투력
  상승 폭이 커진 만큼, 그 재원인 골드(-75%)·성장에너지(-55%) 획득량을
  스테이지 보상 전체에서 함께 낮췄다. 다른 재화(존재력/시간에너지/숙련의
  정수/다이아)는 그대로 뒀다.
- **리버스 재화 초기화 범위 확장** — 다이아는 지금까지 리버스해도 초기화 없이
  구간 지급량만 그대로 누적됐는데, 이제 다른 재화처럼 0으로 초기화된 뒤
  구간 고정값을 새로 받는다. 존재력(EXIST)도 이번에 초기화 대상에 추가됐다
  (다만 별도 재지급은 없다 — 존재력 트리 해금 상태 자체는 그대로 유지되고,
  존재력 재화는 전투로 다시 모으면 된다). 시간에너지만 리버스와 무관하게
  그대로 유지된다. 재화별 처리는 `CurrencyTable`의 `ResetOnRebirth`/
  `RefundOnRebirth` 두 칼럼으로 결정되도록 정리했다(이전엔 다이아만 이
  구조 밖에서 하드코딩돼 있었다).
- CLAUDE.md의 게임 설명을 실제 구현 상태에 맞게 정리(특정 분 단위 진행
  목표 서술 제거 등 — 구체적인 도달 시간 목표는 두지 않는다는 방침으로
  대체).

### 수정
- 전투 화면 좌상단 프로필 칩이 실제 플레이어 이름이 아니라 "플레이어"라는
  고정 라벨을 그대로 보여주던 문제.
- 이름을 아직 정하지 않은 상태(기본값·빈 문자열)로 랭킹에 등록될 수 있던 문제 —
  이제 이름이 없으면 랭킹 등록 자체를 시도하지 않는다.
- 폰트(Pretendard Variable) 전체 글자셋(2MB)을 매번 통째로 내려받고 있던 문제 —
  접속할 때마다 그 다운로드가 끝나기 전까지 대체 글꼴로 보이다가 바뀌는 순간이
  느리거나 느린 회선에서 눈에 띄었을 수 있다. 실제로 쓰는 글자만 필요한 조각으로
  나눠 불러오는 dynamic-subset 버전으로 교체 — 초기 로드 용량이 화면당
  200~300KB 안팎으로 줄었다.

## [v0.2.0] - 2026-09-11

밸런스를 담당하는 데이터 테이블을 전면 개편해 기획자가 엑셀만 고쳐도 게임 밸런스를
직접 조정할 수 있게 됐다. 스테이지 최초 클리어 보상과 화면 비율 고정 같은 실사용성
개선도 함께 들어갔고, 너무 빠듯했던 전체 진행 속도를 여유 있게 재조정했다.

> ⚠️ **저장 데이터 초기화**: 테이블 구조가 크게 바뀌어 이전 버전(v0.1.0) 세이브와
> 호환되지 않는다. 업데이트 후 처음 접속하면 진행 상황이 초기화된다(로드 자체가
> 실패하거나 값이 깨지는 것은 아니고, 안전하게 새 게임으로 시작한다).

### 추가
- **스테이지 최초 클리어 다이아 보상** — 각 스테이지를 계정 기준으로 처음 깰 때만
  다이아를 지급한다. 챕터가 오를수록, 보스 스테이지일수록 지급량이 커진다. 이미
  깬 적 있는 스테이지는(리버스로 다시 지나가도) 재지급되지 않는다. 최초 클리어
  순간에는 일반 처치 데미지 숫자와 구분되는 금색 다이아 배지가 뜬다.
- **화면 비율 고정** — 데스크톱 브라우저 창 크기를 바꿔도 게임 화면이 항상 같은
  세로 프레임 비율로 보인다. 창이 짧으면 프레임 전체가 세로에 맞춰 축소되고, 넓으면
  화면을 채우도록 확대된다. 프레임 바깥은 은은한 비네트 배경으로 채운다. 실제
  모바일 기기에서는 이 제약 없이 화면을 그대로 채운다.
- 플레이어 프로필을 위한 기반 데이터(닉네임, 누적 플레이타임, 현재 회차
  플레이타임)를 준비했다 — 아직 화면에 보여주지는 않는다.

### 변경
- **데이터 테이블 전면 개편** — 밸런스 수치를 실시간 공식으로 계산하던 방식에서
  엑셀에 값을 직접 나열하는 방식으로 바꿨다. 존재력 트리 50노드, 스테이지 200개,
  무기 75종을 비롯해 무기 종류·숙련·등급 등 서로 얽혀 있던 테이블들을 정리·흡수해
  1행짜리 임시 테이블 4개를 없앴고, 화면 곳곳에 하드코딩돼 있던 문구도 전부
  텍스트 테이블로 옮겼다. 플레이 경험은 유지하면서, 이제 기획자가 코드를 건드리지
  않고 엑셀에서 노드 하나·스테이지 하나·무기 하나까지 개별로 조정할 수 있다.
- **전체 진행 속도 하향** — 리버스 해금(존재력 트리 15노드)까지 약 3분이던 것을
  15~20분대로, 타임 하이스트 해금(33노드)까지를 40분 전후로 늦췄다. 최초 클리어
  다이아 보상이 새로 생긴 만큼 리버스 시 지급되는 다이아는 절반으로 줄여 총량이
  과해지지 않게 맞췄다. 다만 처음 켰을 때의 첫 5분 체감 속도는 그대로 유지했다.
- 전투 화면과 성장(컨트롤) 화면의 비율을 정확히 반반으로 맞추고, 몬스터 표시와
  스테이지 정보를 더 크게 키웠다.

### 수정
- 리버스·타임 하이스트 해금 비용이 실제 노드(15번·33번) 비용과 1만큼 어긋나 있던
  자잘한 오차 수정.
- 화면 비율 고정 작업 직후 전투/성장 화면 비율이 창 크기에 따라 들쭉날쭉하던 문제.

## [v0.1.0] - 2026-09-10

프로토타입 초기 버전. 핵심 3시스템(존재력 트리·리버스·타임 하이스트)과 무기/유물
시스템, 성장 탭, 자동 전투 루프까지 방치형 RPG의 한 사이클을 처음부터 끝까지
플레이할 수 있는 상태.

### 추가

- **존재력 트리** — 50노드 단일 선형 트리. 순서대로 해금하며 스탯 상승/재화 지급
  효과를 얻고, 15노드·33노드 해금 시 트리 옆에 리버스·타임 하이스트 특별 해금이
  등장한다.
- **리버스(REBIRTH)** — 스테이지·스탯·숙련을 초기화하고 무기·유물을 소멸/초기화하는
  대신, 그동안 소비한 재화를 전액 환급받고 도달 스테이지에 비례한 다이아를 새로
  받는다. 존재력 트리는 유지. 리버스 회차가 쌓일수록 환급 배율이 올라간다.
- **타임 하이스트** — 시간에너지를 소비해 미래 스테이지의 보상을 미리 수령. 사용할수록
  비용과 쿨타임이 늘어난다.
- **성장 탭** — 5스탯(공격력/공격속도/치명타확률/치명타피해/존재력획득량) 업그레이드,
  무기 종류별(검/창/활) 숙련. 성장에너지가 들어올 때마다 자동으로 가장 싼 스탯을
  올려주는 자동 업그레이드 토글(시연 편의용, 존재력 트리는 자동화 대상 아님).
- **무기 시스템** — 가챠로 획득(3종류×5등급×5단계, 75종), 레벨업, 돌파, 합성. 보유·
  장착 효과가 분리되어 있고 돌파/합성 모두 최소 1개는 항상 남긴다.
- **유물 시스템** — 뽑기, 슬롯 활성화(존재력 트리 해금 노드 수에 연동), 스탯/골드
  획득량/타임 하이스트 쿨타임 등 다양한 효과.
- **오프라인 보상** — 접속을 끊은 동안 전투가 계속된 것으로 계산해 재접속 시 보상 지급.
- **자동 저장** — 진행 상태를 로컬스토리지에 주기적으로 저장, 새로고침해도 유지.
- **온보딩** — 첫 진입 시 웰컴 안내 3단계, 각 시스템 첫 진입 시 짧은 소개 배너.
- **밸런싱 파이프라인** — 모든 수치를 `balance/balance.xlsx` 엑셀 파일에서 관리하고
  `npm run balance`로 게임 데이터에 반영. 기획자가 코드를 건드리지 않고 엑셀만
  고쳐서 밸런스를 조정할 수 있다.
- **다크 미니멀 디자인 시스템** — 색 토큰, 아이콘, 공용 UI 컴포넌트를 전체 화면에 적용.
- 도감 탭은 "설계 완료 · 프로토타입 미구현" 스텁으로 자리만 확보(의도적 범위 밖).
- Vercel 배포 설정, 프로젝트 소개 README, 아키텍처/무기 시스템/디자인 시스템 문서.

### 변경

- 장비 5부위 강화 시스템(무기/투구/갑옷/장갑/신발)을 폐기하고 무기 가챠·레벨업·
  돌파·합성 구조로 재설계.
- 6스탯 중 실제 전투에 영향을 주지 않던 방어력(DEF)을 제거하고 5스탯 체제로 정리.
- 가챠 탭을 "소환"으로 이름을 바꾸고 무기/유물 하위 탭으로 분리.
- 리버스 회차 보너스를 스탯 직접 증가 방식에서 재화 환급 배율 방식으로 재설계.
- 리버스 시 다이아 지급을 공식 계산에서 도달 스테이지 구간별 고정 지급 테이블로 교체.
- 전체 페이스를 여러 차례 재조정 — 첫 리버스(15노드)까지 5~7분, 타임 하이스트
  해금(33노드)까지 11~15분 선에서 안착.
- 밸런싱 테이블 구조 1단계 개편 — 스탯/숙련 업그레이드 비용 곡선을 공용
  `GrowthCurveTable`로 통합, 재화·등급 속성을 담을 공용 테이블(`CurrencyTable`,
  `GradeTable`) 신설. 밸런스 시딩 스크립트를 1회성으로 격리하고 행 추가 전용 도구를
  마련해, 엑셀에서 직접 수정한 값이 스크립트 재실행으로 사라지지 않게 함.

### 수정

- 전투 루프가 화면(컴포넌트) 언마운트 시 멈추던 문제.
- 리버스해도 최고 도달 스테이지가 실시간으로 갱신되지 않고 다음 리버스 시점에만
  반영되던 문제.
- 플레이어가 강해져 적을 한 방에 처치할 수 있게 되면 체력바가 실제 변화를
  보여주지 않고 멈춘 것처럼 보이던 문제, 그리고 가챠를 연속으로 여러 번 뽑으면
  그동안 전투 체력바 갱신이 밀리던 문제.
- 무기 돌파·합성 시 보유 수량이 0개가 될 때까지 소모되던 문제 — 이제 항상 최소
  1개는 남긴다.
- 리버스 시 성장에너지·골드·숙련의 정수가 초기화되지 않고 기존 보유량에 환급분만
  더해져 리버스할수록 재화가 끝없이 누적되던 문제.
