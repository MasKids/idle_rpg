# 프로젝트: 시간 기반 방치형 RPG (웹 프로토타입)

## 목적
취업 포트폴리오용 시연 프로토타입. 상용 게임이 아님.
14일 안에 핵심 3시스템을 동작하는 형태로 보여주는 것이 목표.
"완성도 높은 게임"이 아니라 "설계한 시스템이 실제로 돈다"를 증명하는 것.

## 스택
- Vite + React + TypeScript
- Tailwind CSS
- Zustand (상태관리)
- localStorage (세이브)
- 배포: Vercel
- 대상: 모바일 세로 화면 (데스크톱에서도 세로 프레임 유지)

## 구현 등급
- A = 완전 구현: 존재력 트리, REBIRTH, TIME HEIST
- B = 골격 구현: 전투, 스테이지, 보스, 6스탯 성장, 오프라인 보상
- B- = 간이 구현: 장비 5부위 강화 (골드 소비처 확보용, 공/방만)
- C = 스텁: 가챠, 유물, 무기숙련
  → 탭은 존재하되 "설계 완료 · 프로토타입 미구현" 패널만 표시

B와 B-는 A가 돋보이는 데 필요한 만큼만. 과설계 금지.

## 핵심 3시스템
1. 존재력(EXIST) — 재화이자 업그레이드 트리.
   12노드(4티어 × 3노드). 티어 내 병렬, 티어 간 순차 해금.
   UI는 4x3 격자 + SVG 연결선.
2. REBIRTH — 환생. 스테이지/스탯/장비 초기화,
   소비한 재료는 전액 환급. 존재력 트리는 유지.
3. TIME HEIST — 미래 스테이지 보상을 시간에너지로 미리 수령.

## 재화
- EXIST (존재력): 존재력 트리 해금
- GROWTH_ENERGY (성장에너지): 6스탯 업그레이드
- TIME_ENERGY (시간에너지): TIME HEIST
- GOLD (골드): 장비 강화

## 6스탯
ATK(공격력), DEF(방어력), ASPD(공격속도), CRIT(치명타율),
CRIT_DMG(치명타피해), EXIST_GAIN(존재력 획득량)

## UI 레이아웃 (4존, 위에서부터)
1. 전투 영역 — 전체폭. 반투명 HUD 오버레이
   (좌: 프로필 / 우: 재화 칩 4개)
2. 컨트롤 영역 — 전체폭, 화면 높이 약 44%
3. 하단 메뉴바 — 아이콘+라벨 5개

탭 동작:
- 컨트롤 영역만 교체: 성장, 장비
- 전체화면 전환(뒤로가기 있음): 가챠, 시간/존재력

색상: 기본 파랑 / 가챠·시간 화면 금색 / 컨트롤 영역 드릴다운 청록

## 폴더 구조
src/
  systems/
    exist/       # A
    rebirth/     # A
    timeheist/   # A
    battle/      # B
    growth/      # B
    equipment/   # B-
  components/    # 공용 UI, StubPanel
  data/          # existTree.ts, stages.ts, stats.ts, equipment.ts
  store/         # gameStore.ts
  types/

## 데이터 방침
엑셀→JSON 파이프라인 만들지 말 것.
data/ 아래 TypeScript 상수로 직접 정의.

## 밸런싱 목표
- 첫 REBIRTH 도달까지 약 3분
- 2회차 이후 1~2분
- 총 5~10분 플레이로 3시스템 모두 경험

## 안 만드는 것 (시간이 남아도)
서버, 계정, 랭킹, 사운드, 실제 아트 리소스,
가챠/유물/무기숙련 실구현

## 작업 원칙
- 지시받은 범위만 구현. 요청하지 않은 기능 추가 금지.
- 이미 동작하는 코드는 지시 없이 수정 금지.
- 작업 후 반드시 실행해서 에러 없는지 확인하고 보고할 것.
- 한국어로 응답할 것.
