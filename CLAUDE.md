# 프로젝트: 시간 기반 방치형 RPG (웹 프로토타입)

## 목적
취업 포트폴리오용 시연 프로토타입. 상용 게임이 아님.
14일 안에 핵심 3시스템을 동작하는 형태로 보여주는 것이 목표.
"완성도 높은 게임"이 아니라 "설계한 시스템이 실제로 돈다"를 증명하는 것.

## 스택
- Vite + React + TypeScript
- Tailwind CSS
- Zustand (상태관리)
- localStorage (세이브)
- 배포: Vercel
- 대상: 모바일 세로 화면 (데스크톱에서도 세로 프레임 유지)

## 구현 등급
- A = 완전 구현: 존재력 트리, REBIRTH(리버스), TIME HEIST,
  무기 시스템(가챠/레벨업/돌파/합성), 유물, 무기 숙련
- B = 골격 구현: 전투, 스테이지, 보스, 5스탯 성장, 오프라인 보상
- C = 스텁: 도감
  → 탭은 존재하되 "설계 완료 · 프로토타입 미구현" 패널만 표시

B는 A가 돋보이는 데 필요한 만큼만. 과설계 금지.

무기/유물/가챠 시스템의 상세 설계는 [docs/WEAPON_SYSTEM.md](docs/WEAPON_SYSTEM.md) 참고.

## 핵심 3시스템
1. 존재력 트리 — 아래 "시스템 1: 존재력 트리" 절 참고.
2. REBIRTH — 환생. 스테이지/스탯 초기화, 무기 전부 소멸, 유물 전부 초기화,
   소비한 재료(숙련의 정수 등)는 전액 환급. 존재력 트리는 유지.
3. TIME HEIST — 미래 스테이지 보상을 시간에너지로 미리 수령.

## 시스템 1: 존재력 트리 (A)
진입: 하단 메뉴 "존재력" 탭 → 전체화면

구조:
- 단일 선형 트리, 총 50노드. order 순서대로만 해금 (분기 없음)
- 중앙 세로 "시간축" + 노드 좌우 지그재그 배치, 세로 스크롤
- 5티어 × 10노드 = 50. 티어는 시각 구분용
- 해금 판정: order <= unlockedCount + 1
- 노드 이름은 미확정. 자동 생성 문자열 사용 (예: "T1-3")
  나중에 이름 배열만 교체할 수 있게 분리해둘 것
- 트리 진행 방향은 아래에서 위로 (1번 노드가 최하단, 50번이 최상단)
- 화면 진입 시 초기 스크롤 위치는 최하단(1번 노드)
- 위로 스크롤하며 상위 노드를 확인하는 구조

노드는 하드코딩하지 말고 data/existTree.ts 에서 계산식으로 생성한다. balance.xlsx의
ExistTreeTable에 50개 노드를 전부 나열하지 않고 **order 구간(OrderFrom~OrderTo) 단위 행**만
정의해두고, 노드 하나하나의 비용/효과는 그 노드가 속한 행의 계수로 계산한다.
5티어(10노드씩, 시각 구분용)는 각각 2개 행(소구간)으로 나뉘어 있어 한 티어 안에서도
서로 다른 효과가 섞인다:
- 1티어(1~10): 1~7=ATK, 8~10=CRIT
- 2티어(11~20): 11~16=ASPD, 17~20=재화 지급(시간에너지)
- 3티어(21~30): 21~26=CRIT_DMG, 27~30=재화 지급(숙련의 정수)
- 4티어(31~40): 31~37=ATK(1티어 ATK 구간의 연장 공식), 38~40=재화 지급(숙련의 정수, 3티어 구간의 연장 공식)
- 5티어(41~50): 41~46=EXIST_GAIN, 47~50=ASPD(2티어 ASPD 구간의 연장 공식)
- cost = floor(행.CostBase × 행.CostGrowthRate ^ (order − 행 시작 order))
  — 모든 행의 CostBase는 전체를 관통하는 단일 지수곡선 `floor(10 × 1.35^(order-1))`에서
  뽑아 쓰므로, 소구간 경계에서도 비용이 끊기지 않고 이어진다.
- 스탯 상승량 / 재화 지급량 = 행.ValueBase + (order − 행 시작 order) × 행.ValuePerNode
  — 같은 효과(ATK, ASPD)가 두 번째 행에서 다시 나올 때는 그 효과의 "첫 행" 공식을
  그대로 연장해 값이 이어지게 했다(중간에 다른 효과 행이 끼어 있어도 하나로 이어진 곡선처럼 동작).
- 행별 CostBase/ValueBase/ValuePerNode는 전부 balance.xlsx에서 조정

특별 해금 (트리 소속 아님, 트리 옆 여백에 등장):
- 트리 15개 해금 시 → 15번 노드 옆에 [리버스] 등장
- 트리 33개 해금 시 → 33번 노드 옆에 [타임 하이스트] 등장
- EXIST 지불해 해금하면 기능 활성화
- 조건 미달 시 표시되지 않음. 화면 고정 아니고 스크롤 따라 만남
- 배열로 정의해 확장 가능하게

## 재화 (6종 — 획득처 → 소비처)
- DIAMOND (다이아): 초기 지급 + 리버스 시 도달 스테이지 기준 신규 지급(환급이 아님,
  `RebirthRewardTable`의 구간별 고정값 — 예: 50스테이지 이상 도달 후 리버스하면 700) → 무기 가챠
- EXIST (존재력): 전투 처치 보상(EXIST_GAIN 배율 적용) → 존재력 트리 해금, 리버스/
  타임 하이스트 특별 해금
- GROWTH_ENERGY (성장에너지): 전투 처치 보상 → 6스탯 업그레이드
- MASTERY_ESSENCE (숙련의 정수): 존재력 트리 3·4티어 일부(27~30, 38~40번 노드) 해금 →
  무기 종류별(검/창/활) 숙련 업그레이드
- TIME_ENERGY (시간에너지): 보스 스테이지 처치(챕터가 오를수록 지급량 증가) + 유물
  중복 획득 시 소량 환급 + 존재력 트리 2티어 일부(17~20번 노드) 해금 → 유물 뽑기,
  타임 하이스트 (소비처가 재화를 공유하므로 총량이 부족해지지 않도록 보스 보상을
  챕터별로 늘려뒀다)
- GOLD (골드): 전투 처치 보상 → 무기 레벨업

## 5스탯
ATK(공격력), ASPD(공격속도), CRIT(치명타율), CRIT_DMG(치명타피해), EXIST_GAIN(존재력 획득량)

DEF(방어력)는 플레이어 피격 개념이 없어 전투에 전혀 영향을 주지 않는 죽은 스탯이라
제거했다(존재력 트리 2티어도 DEF에서 ASPD/시간에너지로 교체). 유물 "신속의 유물"도
같은 이유로 STAT_DEF → STAT_ASPD로 교체됨.

## UI 레이아웃 (4존, 위에서부터)
1. 전투 영역 — 전체폭. 반투명 HUD 오버레이
   (좌: 프로필 / 우: 재화 칩 4개 — 다이아/존재력/성장에너지/골드.
   숙련의 정수는 성장 탭, 시간에너지는 유물 탭·타임 하이스트 모달에서만 표시)
2. 컨트롤 영역 — 전체폭, 화면 높이 약 44%
3. 하단 메뉴바 — 아이콘+라벨 5개 (성장 / 무기고 / 가챠 / 존재력 / 도감)

탭 동작:
- 컨트롤 영역만 교체: 성장
- 전체화면 전환: 무기고, 가챠, 존재력, 도감
  · 전투 영역 + 컨트롤 영역을 덮는 전체화면
  · 하단 메뉴바는 항상 유지됨 (덮지 않음)
  · 좌상단에 뒤로가기 버튼
  · 무기고 탭은 상단에 [장비] [유물] 하위 탭을 둔다 (상세: docs/WEAPON_SYSTEM.md)

색상: 기본 파랑 / 가챠·시간 화면 금색 / 컨트롤 영역 드릴다운 청록

## 폴더 구조
src/
  systems/
    exist/       # A
    rebirth/     # A
    timeheist/   # A
    weapon/      # A (무기, 무기 가챠, 무기 숙련)
    relic/       # A (유물)
    battle/      # B
    growth/      # B
  components/    # 공용 UI, StubPanel
  data/          # existTree.ts, stages.ts, stats.ts, mastery.ts, balance.ts, uiStrings.ts
  store/         # gameStore.ts
  types/

## 데이터 방침
밸런싱 수치는 balance/balance.xlsx → (npm run balance) → src/data/balance.json →
src/data/balance.ts(BALANCE 객체) 파이프라인으로 관리한다. 절차/주의사항은
balance/README.md 참고. 노드/스테이지 등 "구조를 생성하는 로직" 자체는 여전히
data/ 아래 TypeScript로 정의하고, 그 로직이 쓰는 숫자만 balance.ts에서 가져온다.

## 밸런싱 목표
- 첫 리버스까지 약 3분 (트리 15노드 도달)
- 2회차 이후 1~2분
- 총 5~10분 플레이로 3시스템 모두 경험

## 안 만드는 것 (시간이 남아도)
서버, 계정, 랭킹, 사운드, 실제 아트 리소스

## 작업 원칙
- 지시받은 범위만 구현. 요청하지 않은 기능 추가 금지.
- 이미 동작하는 코드는 지시 없이 수정 금지.
- 작업 후 반드시 실행해서 에러 없는지 확인하고 보고할 것.
- 한국어로 응답할 것.
