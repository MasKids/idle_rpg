# 프로젝트: 시간 기반 방치형 RPG (웹 프로토타입)

## 목적
게임 제목은 **Re: NOVA**(부제 "Nonlinear Oscillation Void Apparatus" — NOVA를
풀어쓴 backronym). 취업 포트폴리오용 시연 프로토타입. 상용 게임이 아님.

**시간을 자원으로 다루는 방치형 RPG.** 성장의 중심 축이 스탯이나 장비가 아니라
"시간을 어떻게 다루는가"에 있다는 게 이 게임의 설계 의도다. 세 가지 시간 관련
시스템이 그 축을 이룬다:
- **존재력 트리** — 순서대로만 해금되는 단일 트리를 시간을 거슬러 올라가듯
  타고 오르며 영구 성장한다
- **REBIRTH(리버스)** — 시간을 되돌려 처음부터 다시 시작하되, 그 대가로 다음
  회차를 더 빠르게 만드는 재화를 얻는다
- **TIME HEIST(타임 하이스트)** — 미래 스테이지의 보상을 시간에너지를 써서
  앞당겨 수령한다

이 세 시스템은 "깊이 도달 → 리버스 → 다이아 획득 → 가챠 레벨 상승 → 더 좋은
무기 → 더 깊이 도달"로 이어지는 성장 루프를 중심으로 서로 맞물린다. 전투·5스탯
성장·무기·가챠 같은 통상적인 방치형 요소는 이 루프를 체감시키기 위한 받침대이지,
그 자체가 이 프로토타입이 증명하려는 대상은 아니다.

"완성도 높은 게임"이 아니라 "설계한 시스템이 실제로 동작한다"를 증명하는 것이
목표다.

## 스택
- Vite + React + TypeScript
- Tailwind CSS
- Zustand (상태관리)
- localStorage (세이브)
- 배포: Vercel
- 대상: 모바일 세로 화면 (데스크톱에서도 세로 프레임 유지)

## 구현 등급
- A = 완전 구현: 존재력 트리, REBIRTH(리버스), TIME HEIST,
  무기 시스템(가챠/레벨업/돌파/합성), 유물, 무기 숙련
- B = 골격 구현: 전투, 스테이지, 보스, 5스탯 성장, 오프라인 보상
- C = 스텁: 도감
  → 탭은 존재하되 "설계 완료 · 프로토타입 미구현" 패널만 표시

B는 A가 돋보이는 데 필요한 만큼만. 과설계 금지.

무기/유물/가챠 시스템의 상세 설계는 [docs/WEAPON_SYSTEM.md](docs/WEAPON_SYSTEM.md) 참고.

## 핵심 3시스템
1. 존재력 트리 — 아래 "시스템 1: 존재력 트리" 절 참고.
2. REBIRTH — 환생. 스테이지/스탯 초기화, 무기 전부 소멸, 유물 전부 초기화,
   성장에너지/골드/숙련의 정수/다이아는 0으로 초기화된 뒤 도달 스테이지 구간별
   고정 지급량만큼 새로 지급된다(누적 소비량 환급 방식이 아님). 성장에너지·골드·
   숙련의 정수는 여기에 리버스 횟수 배율까지 곱해서 지급되지만(회차가 쌓일수록
   배율 상승, 기본 회차당 +5%p, 최대 ×3.0), 다이아는 구간별 고정값만 받고 이
   배율은 적용되지 않는다. 존재력(EXIST)은 초기화만 되고 재지급은 없다(전투로
   다시 모으는 재화). 시간에너지는 리버스와 무관하게 그대로 유지된다. 존재력
   트리(노드 해금 상태)는 유지.
3. TIME HEIST — 미래 스테이지 보상을 시간에너지로 미리 수령.

## 시스템 1: 존재력 트리 (A)
진입: 하단 메뉴 "존재력" 탭 → 전체화면

구조:
- 단일 선형 트리, 총 250노드(v0.4.0부터 — 50→250 확장). order 순서대로만 해금 (분기 없음)
- 중앙 세로 "시간축" + 노드 좌우 지그재그 배치, 세로 스크롤
- 25티어 × 10노드 = 250. 티어는 시각 구분용(원본 5티어 × 10노드와 같은
  "청크 크기"를 유지해, 확장 후에도 스크롤 중 티어가 바뀌는 빈도가 비슷하게
  느껴지도록 했다)
- 해금 판정: order <= unlockedCount + 1
- 노드 이름은 1~50번은 이미 확정돼 있고(StringTable), 51~250번은 아직 미확정 —
  "T{tier}-{n}" 자동 생성 패턴 문자열이 그대로 들어있다. 이름이 정해지면
  StringTable의 NameStringId 값만 바꾸면 됨
- 트리 진행 방향은 아래에서 위로 (1번 노드가 최하단, 250번이 최상단)
- 화면 진입 시 초기 스크롤 위치는 최하단(1번 노드)
- 위로 스크롤하며 상위 노드를 확인하는 구조

노드는 하드코딩하지 않고 balance.xlsx의 ExistTreeTable에서 읽는다(data/existTree.ts).
250개 노드 전부가 개별 행으로 나열돼 있어 **런타임 계산식은 없다** — 구간(OrderFrom~
OrderTo) 단위로 압축해 공식으로 계산하던 방식은 폐기됐다(docs/TABLE_REDESIGN.md
2.2절). 각 행이 order/tier/효과 종류(스탯 or 재화 지급)/수치(Value)/비용(Cost)을
그대로 담고 있고, 코드는 order로 해당 행을 찾아 조회만 한다.

티어별 효과 구성(현재 ExistTreeTable 실제 값 기준 — 스탯형 노드의 Value는 v0.3.0부터
퍼센트다. "밸런싱 방침" 절 참고):
- 1티어(1~10): 1~7=ATK%(3→9), 8~10=CRIT%(5→7)
- 2티어(11~20): 11~16=ASPD%(5→10), 17~20=재화 지급(시간에너지 8→14)
- 3티어(21~30): 21~26=CRIT_DMG%(5→10), 27~30=재화 지급(숙련의 정수 31→34)
- 4티어(31~40): 31~37=ATK%(10→22, 1티어 ATK 구간보다 크게), 38~40=재화 지급(숙련의 정수 42→44)
- 5티어(41~50): 41~46=EXIST_GAIN%(4→9), 47~50=ASPD%(15→24, 2티어 ASPD 구간보다 크게)
- 6~25티어(v0.4.0 확장분): 1~5티어의 밴드 형태(스탯 7+3+6+6+7+6+4개, 재화
  4+4+3개 — 스탯:재화 = 78:22 비율)를 그대로 "사이클"로 4번 더 반복한다.
  사이클마다 5스탯(ATK/ASPD/CRIT/CRIT_DMG/EXIST_GAIN)이 한 칸씩 돌아가며 그
  사이클의 "대표 스탯"을 맡아 25티어 전체에서 고르게 분포하고, 시간에너지/
  숙련의 정수 지급 노드도 각 사이클마다 정해진 자리에 고정으로 등장해 트리
  전체에 고르게 흩어진다. 노드 값은 사이클마다 정해둔 목표 구간 안에서만
  완만히 증가한다(사이클을 거듭할수록 구간 증가량 자체가 등차수열을 이루도록
  설계 — 순수 지수 증가보다 훨씬 완만하다). 정확한 생성 규칙과 설계 근거는
  scripts/seed/seed-exist-tree-250.mjs 참고.

250노드 전부 뚫었을 때 스탯별 퍼센트 합계: ATK +1366%, ASPD +1256%, CRIT +1571%,
CRIT_DMG +1679%, EXIST_GAIN +1794%.

노드별 비용은 티어가 올라갈수록, 같은 티어 안에서도 order가 커질수록 가파르게
증가한다(1번 노드 12 → 50번 노드 9,927 → 250번 노드 약 6.4억). 51번부터는
노드당 증가율을 50번까지의 평균(1.13)에서 시작해 250번에 가까워질수록 점점
낮춰(최저 1.02) 후반이 지나치게 비싸지지 않게 했다. 비용/수치는 노드 하나하나
balance.xlsx에서 개별 조정 가능.

특별 해금 (트리 소속 아님, 트리 옆 여백에 등장):
- 트리 20개 해금 시 → 20번 노드 옆에 [리버스] 등장(v0.4.0에서 15→20 —
  250노드 체계에서도 여전히 초반에 열리도록, 기존 1~50번 구간 안에서 조금만
  뒤로 미뤘다)
- 트리 45개 해금 시 → 45번 노드 옆에 [타임 하이스트] 등장(v0.4.0에서 33→45,
  같은 이유)
- EXIST 지불해 해금하면 기능 활성화
- 조건 미달 시 표시되지 않음. 화면 고정 아니고 스크롤 따라 만남
- 배열로 정의해 확장 가능하게

유물 슬롯: 존재력 트리 해금 수 기준(RelicSlotTable)으로 늘어난다 — v0.4.0에서
5슬롯(10노드 간격) → 8슬롯(30노드 간격, 30/60/90/120/150/180/210/240)으로
확장했다. 슬롯 수는 RelicSlotTable 행 수로 자동 결정되는 값이라(코드에 5가
하드코딩돼 있지 않음) 나중에 더 늘리고 싶으면 행만 추가하면 된다.

## 재화 (6종 — 획득처 → 소비처)
- DIAMOND (다이아): 초기 지급 + 스테이지 최초 클리어 시 지급(계정 기준 처음 깰 때만,
  챕터가 오를수록·보스 스테이지일수록 증가 — `StageTable.FirstClearDiamond`) +
  리버스 시 도달 스테이지 구간별 고정값 신규 지급(환급이 아님 — `RebirthRewardTable`)
  → 무기 가챠
- EXIST (존재력): 전투 처치 보상(EXIST_GAIN 배율 적용) → 존재력 트리 해금, 리버스/
  타임 하이스트 특별 해금
- GROWTH_ENERGY (성장에너지): 전투 처치 보상 → 5스탯 업그레이드
- MASTERY_ESSENCE (숙련의 정수): 존재력 트리 3·4티어 일부(27~30, 38~40번 노드) 해금 →
  무기 종류별(검/창/활/도끼/지팡이, v0.4.0에서 2종 추가) 숙련 업그레이드
- TIME_ENERGY (시간에너지): 보스 스테이지 처치(챕터가 오를수록 지급량 증가) + 유물
  중복 획득 시 소량 환급 + 존재력 트리 2티어 일부(17~20번 노드) 해금 → 유물 뽑기,
  타임 하이스트 (소비처가 재화를 공유하므로 총량이 부족해지지 않도록 보스 보상을
  챕터별로 늘려뒀다)
- GOLD (골드): 전투 처치 보상 → 무기 레벨업

재화별 리버스 처리는 코드가 아니라 `CurrencyTable`의 두 칼럼이 결정한다 —
`ResetOnRebirth`(0으로 초기화할지)와 `RefundOnRebirth`(초기화 후 구간별 고정값을
새로 채울지). TIME_ENERGY만 둘 다 false(리버스와 완전히 무관). EXIST는
`ResetOnRebirth`만 true(초기화되지만 재지급은 없음 — 전투로 다시 모으는 재화).
GROWTH_ENERGY/GOLD/MASTERY_ESSENCE/DIAMOND는 둘 다 true(초기화 후 `RebirthRewardTable`의
구간별 고정값으로 채워짐 — 다이아만 리버스 횟수 배율이 적용되지 않는다).

## 5스탯
ATK(공격력), ASPD(공격속도), CRIT(치명타율), CRIT_DMG(치명타피해), EXIST_GAIN(존재력 획득량)

DEF(방어력)는 플레이어 피격 개념이 없어 전투에 전혀 영향을 주지 않는 죽은 스탯이라
제거했다(존재력 트리 2티어도 DEF에서 ASPD/시간에너지로 교체). 유물 "신속의 유물"도
같은 이유로 STAT_DEF → STAT_ASPD로 교체됨.

## UI 레이아웃 (4존, 위에서부터)
1. 전투 영역 — 전체폭. 반투명 HUD 오버레이
   (좌: 프로필 / 우: 재화 칩 4개 — 다이아/존재력/성장에너지/골드.
   숙련의 정수는 성장 탭, 시간에너지는 유물 탭·타임 하이스트 모달에서만 표시)
2. 컨트롤 영역 — 전체폭, 화면 높이 약 44%
3. 하단 메뉴바 — 아이콘+라벨 5개 (성장 / 무기고 / 가챠 / 존재력 / 도감)

탭 동작:
- 컨트롤 영역만 교체: 성장
- 전체화면 전환: 무기고, 가챠, 존재력, 도감
  · 전투 영역 + 컨트롤 영역을 덮는 전체화면
  · 하단 메뉴바는 항상 유지됨 (덮지 않음)
  · 좌상단에 뒤로가기 버튼
  · 무기고 탭은 상단에 [장비] [유물] 하위 탭을 둔다 (상세: docs/WEAPON_SYSTEM.md)

색상: 기본 파랑 / 가챠·시간 화면 금색 / 컨트롤 영역 드릴다운 청록

## 폴더 구조
src/
  systems/
    exist/       # A
    rebirth/     # A
    timeheist/   # A
    weapon/      # A (무기, 무기 레벨업/돌파/합성, 무기 숙련)
    relic/       # A (유물)
    gacha/       # A (무기/유물 가챠 통합 UI — "소환" 탭)
    battle/      # B
    growth/      # B
    onboarding/  # 웰컴 온보딩, 시스템별 첫 진입 배너, 탭 뱃지
  components/    # 공용 UI, StubPanel
  data/          # existTree.ts, stages.ts, stats.ts, mastery.ts, balance.ts, uiStrings.ts
  store/         # gameStore.ts, gameStateStorage.ts
  types/

## 데이터 방침
밸런싱 수치는 balance/balance.xlsx → (npm run balance) → src/data/balance.json →
src/data/balance.ts(테이블별 조회 헬퍼) 파이프라인으로 관리한다. 절차/주의사항은
balance/README.md 참고.

존재력 트리 250노드, 스테이지 300개, 무기 150종 등은 더 이상 공식으로 계산하지
않는다 — balance.xlsx에 개별 행으로 전부 나열돼 있고, data/ 아래 TypeScript는
그 행을 order/stage/type 등으로 찾아 조회만 한다(런타임 계산 로직 없음). 지그재그
배치(lane)처럼 order로부터 결정론적으로 계산 가능한 것만 코드에 남아있다.

엑셀을 처음부터 새로 만드는 1회성 시딩 스크립트(scripts/seed/)는 balance.xlsx가
이미 있으면 실행을 거부한다 — 평소 밸런싱 흐름에서 쓰지 않는다. 기존 시트에 행을
하나만 추가하고 싶을 때는 scripts/append-row.mjs를 쓴다(다른 시트·다른 행은
건드리지 않고 지정한 시트 끝에 행 하나만 추가).

## 데이터 테이블 (balance.xlsx)
`#TableDefine`(칼럼 명세)·`#EnumDefine`(enum 정의) 두 메타 시트를 제외하고
16개 데이터 테이블이 있다:

- **StageTable** — 스테이지 300개(30챕터×10, v0.4.0에서 200→300) 전부 개별 행. 적 HP/공격력,
  처치 보상, 최초 클리어 다이아
- **ExistTreeTable** — 존재력 트리 250노드 전부 개별 행. order/tier/효과/수치/비용
- **StatTable** — 5스탯 기본값·레벨당 증가량·성장 곡선 키
- **WeaponTable** — 무기 150종(5종류×6등급×5단계, v0.4.0에서 75종→150종 확장)
  전부 개별 행. 종류별 특성 스탯, 등급 이름/색상, 기본 공격력, 보유효과(퍼센트)·
  장착효과(깡스탯)
- **WeaponBreakthroughTable** — 돌파 단계별 필요 중복 수·레벨 상한 보너스
- **GachaTable** — 가챠 레벨별 등급/티어 확률과 다이아 비용
- **RelicTable** — 유물별 등급/효과 종류/효과량/효과 수치 종류(깡스탯/퍼센트,
  유물마다 다름)/뽑기 가중치
- **RelicSlotTable** — 유물 슬롯별 해금 조건(존재력 트리 해금 수)
- **FeatureUnlockTable** — 리버스/타임 하이스트 특별 해금 조건과 비용
- **TimeHeistTable** — 사용 횟수별 비용/쿨타임/보상 배율
- **RebirthRewardTable** — 도달 스테이지 구간별 리버스 지급량(다이아·성장에너지·
  골드·숙련의 정수). 성장에너지/골드/숙련의 정수는 여기에 리버스 횟수 배율까지
  곱해서 지급(다이아는 배율 미적용) — 배율 자체는 CommonTable의
  RebirthCountBonusPerRun/MaxRebirthCountMultiplier로 계산
- **PatchNoteTable** — 인게임 패치노트 한 줄 단위 행. 버전/출시일/분류(추가·변경·
  수정)/문구/같은 버전 내 순서. 프로필 패널 → 패치노트에서 열람(버전 규칙: 릴리스
  발행 시 여기에도 같이 추가 — "변경 이력 관리" 절 참고)
- **CurrencyTable** — 재화별 리버스 초기화/환급/HUD 노출 여부
- **GrowthCurveTable** — 스탯·무기 레벨업 등이 공유하는 비용 성장 곡선
- **CommonTable** — 위 테이블에 속하지 않는 공용 상수(자동저장 주기, 오프라인
  보상 상한 등)
- **StringTable** — 화면에 쓰이는 모든 한글/영문 텍스트

테이블 구조 개편 배경은 docs/TABLE_REDESIGN.md 참고.

## 밸런싱 방침
- 콘텐츠 간 시너지가 나도록 깡스탯과 퍼센트를 구분한다 — 최종 스탯 =
  (기본값 + 깡스탯 합계) × (1 + 퍼센트 합계). 6스탯 업그레이드·무기 장착 효과는
  깡스탯, 존재력 트리·무기 보유 효과·무기 숙련은 퍼센트, 유물은 혼합(유물별로
  다름)
- 각 콘텐츠는 고유한 성장 축을 담당한다 — 서로 다른 곱연산 요인이라 하나만
  투자해서는 최종 전투력이 크게 오르지 않고, 여러 콘텐츠를 같이 키워야 시너지가
  난다
- 리버스 회차가 쌓일수록 리버스 보상 배율이 올라가 이후 회차는 더 빠르게 진행되는 구조
- 구체적인 도달 시간 목표는 두지 않는다

## 안 만드는 것 (시간이 남아도)
서버, 계정, 사운드, 실제 아트 리소스.
(랭킹은 별도로 구현 검토 중이라 이 목록에서 제외 — 아직 확정된 설계 없음)

## 작업 원칙
- 지시받은 범위만 구현. 요청하지 않은 기능 추가 금지.
- 이미 동작하는 코드는 지시 없이 수정 금지.
- 작업 후 반드시 실행해서 에러 없는지 확인하고 보고할 것.
- 한국어로 응답할 것.

## 변경 이력 관리
- 기능 추가, 변경, 버그 수정 작업 후에는 CHANGELOG.md의 [Unreleased] 섹션에
  항목을 추가할 것
- 사용자 관점에서 읽히게 작성. 내부 리팩터링은 간략히
- 릴리스 발행 시 [Unreleased] 내용을 새 버전 섹션으로 이동
- 릴리스 발행 시 CHANGELOG.md뿐 아니라 balance.xlsx의 PatchNoteTable에도 그
  버전의 인게임 패치노트를 추가할 것 — CHANGELOG.md를 그대로 옮기지 말고
  플레이어가 체감할 항목만 1줄(20자 내외)로 간결하게 추려서 작성한다(append-row.mjs
  사용, 절차는 balance/README.md 참고). 내부 리팩터링·문서 정리 등 플레이어가
  체감할 수 없는 항목은 아예 뺀다.

## 버전 규칙
- v0.X.0 : 큰 기능 추가 또는 시스템 개편
- v0.X.Y : 버그 수정, 밸런싱 조정
- 프로토타입 단계이므로 앞자리는 0 유지
- **릴리스 시(v0.X.0/v0.X.Y 무관) 세이브 버전(`src/store/gameStateStorage.ts`의
  `SAVE_VERSION`)을 반드시 함께 올린다.** 저장 구조가 실제로 바뀌었는지는 따지지
  않는다 — 마이그레이션을 만들지 않는 프로토타입 특성상 매 릴리스를 깨끗한
  상태에서 시작하는 쪽을 원칙으로 삼는다. 손으로 상수를 고치지 말고
  `node scripts/bump-save-version.mjs vX.Y.Z "사유"`로 올린다(절차 상세는
  docs/RELEASE.md 1·3절).
